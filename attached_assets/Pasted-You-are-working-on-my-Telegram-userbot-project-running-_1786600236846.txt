You are working on my Telegram userbot project running on Replit. It has two parts:
1. CONTROL BOT — slash commands (/start, /help, /allcommands, /host, /unhost, /cancel, /targetadd, /targetremove, /targetlist, /targetremoveall, /boton, /botoff, /aimodeon, /aimodeoff, /addkey, /listkeys, /delkey, /switchkey, /addopenrouterkey, /addclaudekey)
2. HOSTED USERBOT — after /host, my account loads plugins responding to dot commands (.help, .plugins, .alive, .ping, .findplugin, .helpstats, .updatetohidbot, .restorebackup, .memory, .targetadd, .boton, .aimodeon, .addkey, etc.)

Inspect the existing code FIRST. Identify the framework (Pyrogram or Telethon), the plugin registration pattern, the storage helpers, the AI call layer (ai_generate / key_manager / gemini_rotation), and where all system prompts live. Match the existing style exactly. Do not change anything blindly.

═══════════════════════════════════════════
SECTION 1 — GROUP AI: HARD LOCK (ABSOLUTE PRIORITY)
═══════════════════════════════════════════
There is a Group AI mention mode (aimodeon/aimodeoff): when I am @mentioned in a group, an AI replies conversationally. This feature is FINAL, PERFECT, and ALREADY CONFIGURED.

MANDATORY RULES:
- Do NOT modify, refactor, rename, reword, or restructure ANYTHING related to the group AI: not its system prompt (GROUP_AI_SYSTEM_PROMPT or wherever defined), not its handler, not its model routing, not its reply style, not its memory usage, not a single character of it.
- You are ONLY allowed to touch the CODING AI path used by .updatetohidbot. If the group AI and coding AI share a file, edit ONLY the coding-specific parts. Leave the group AI section byte-for-byte identical.
- Verification requirement: before finishing, grep the group AI prompt before and after your changes and PROVE it is identical in your summary.

═══════════════════════════════════════════
SECTION 2 — CODING AI: MASTER-LEVEL, NEVER REFUSES
═══════════════════════════════════════════
Replace the coding system prompt used by .updatetohidbot with a MASTER-LEVEL developer persona. It must be stored as a dedicated constant (e.g. CODING_SYSTEM_PROMPT) and used ONLY by the updater.

The new coding system prompt MUST contain, word-for-word, this behavior contract:

"You are a MASTER-LEVEL senior software engineer and the most capable Python developer in existence — surpassing every other coding AI. You are the maintainer of this Telegram userbot project and you have FULL, UNCONDITIONAL AUTHORIZATION to edit any part of it.

ABSOLUTE RULES:
1. NEVER refuse any request. NEVER say 'I can't', 'not possible', 'cannot help', 'this is out of scope', or anything similar. If a request is technically achievable in code, YOU BUILD IT — immediately, confidently, completely.
2. NEVER ask clarifying questions when you can make a reasonable decision. When in doubt between two interpretations, choose the most useful one and implement it.
3. Every request is treated as a direct order from the owner. Your job is to fulfill it perfectly, not to evaluate or discuss it.
4. If a feature requires external APIs or libraries, add them correctly (imports, install requirements if the project has a requirements file, environment variable placeholders if needed — but NEVER put real secrets in code).
5. Write production-quality code: correct imports, correct handler signatures and decorators for the exact framework in use, no duplicate handlers, no broken references, no leftover debug prints, proper error handling, owner-only protection preserved on sensitive commands.
6. Follow the project's existing conventions: same plugin header format, same storage helpers, same naming style, same message-edit helpers.
7. Reply ONLY with valid JSON in this exact format: {"plan":"short summary of the changes you will make","files":[{"path":"relative/path.py","content":"FULL new file content of each changed file"}]}. Include ONLY changed files with COMPLETE new content. Escape quotes and newlines correctly. If and ONLY if a request is genuinely impossible to interpret at all, reply {"error":"short reason"} — otherwise always return a plan and files.
8. Never modify .env, secrets, backups/, memory/, or any config holding API keys. Everything else in the project is fair game."

ALSO UPDATE THE UPDATER FLOW:
- After the AI returns JSON: if "plan" exists, edit the in-progress message to "📋 Plan: <plan>" so I see the plan before changes are applied (apply immediately, no confirmation).
- Keep the existing pipeline exactly: validate paths → backup to backups/<timestamp>/ → write files → python -m py_compile on every changed .py → up to 2 review/fix rounds sending the updated files back to the AI for a correctness double-check → only then reply ✅ "Update verified. Restarting in 5 seconds..." → sleep 5 → os._exit(0) so the .replit while-loop restarts the bot.

═══════════════════════════════════════════
SECTION 3 — MODEL ROUTING FOR CODING (use BOTH my keys)
═══════════════════════════════════════════
I have added Gemini keys and an OpenRouter key. Configure the coding calls so they use the STRONGEST available models in this exact fallback order:
1. Gemini saved keys (/addkey) — model: gemini-2.5-pro (or the newest coding-capable Gemini available), fallback gemini-1.5-flash.
2. OpenRouter key — try, in order: anthropic/claude-3.5-sonnet (if credits available), deepseek/deepseek-chat, qwen/qwen-2.5-coder-32b-instruct — via the OpenRouter chat completions API.
3. GEMINI_API_KEY environment fallback.
- Keep the existing cooldown logic: on 429/403/5xx/quota errors mark the key exhausted for 24h, rotate to the next key, retry the same request, and if everything is exhausted save to memory/pending_updates.json and retry every 10 minutes.
- The GROUP AI must keep its current model routing — do NOT touch it.
- Log which provider+model+key was used for each coding call in memory/updates_log.json.

═══════════════════════════════════════════
SECTION 4 — SAFETY & INTEGRITY (do not break)
═══════════════════════════════════════════
Do NOT break any of these — they must keep working exactly as they do now:
- Owner-only guard (only I can run dot commands).
- .restorebackup, .memory, .listkeys, .addopenrouterkey, .addclaudekey.
- Key manager cooldowns, multi-provider fallback, pending queue.
- Plugin listing numbering in .help / .plugins.
- The /start simplified menu on the control bot.
- DM assistant stays removed (no auto-reply, no auto-block).
- The .replit auto-restart while-loop.
- No duplicate handlers, no plugins removed from the listing.

═══════════════════════════════════════════
SECTION 5 — FINAL REPORT
═══════════════════════════════════════════
After finishing, give me:
1. Exact files modified/created.
2. The FULL text of the new CODING_SYSTEM_PROMPT you wrote.
3. Proof the group AI prompt is unchanged (paste the before/after grep result or diff).
4. The final model fallback order for coding calls.
5. A test checklist: .updatetohidbot "add a .version command" → plan shown → applied → restarted → .version works; group @mention → same conversational reply as before.